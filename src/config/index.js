/**
 * Configuration Exports
 */

const { PROPFIRMS, PROPFIRM_CHOICES } = require('./propfirms');
const {
  ACCOUNT_STATUS,
  ACCOUNT_TYPE,
  ORDER_STATUS,
  ORDER_TYPE,
  ORDER_SIDE,
  FUTURES_SYMBOLS
} = require('./constants');

module.exports = {
  PROPFIRMS,
  PROPFIRM_CHOICES,
  ACCOUNT_STATUS,
  ACCOUNT_TYPE,
  ORDER_STATUS,
  ORDER_TYPE,
  ORDER_SIDE,
  FUTURES_SYMBOLS
};
